@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Personas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .container {
            margin-top: 50px;
        }

        h2 {
            color: #007bff;
            font-size: 2rem;
            text-align: center;
            margin-bottom: 30px;
        }

        .btn-success {
            width: 200px;
            font-size: 1.1rem;
            padding: 10px;
        }

        .info-text {
            text-align: center;
            font-size: 1.2rem;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Listado de Personas</h2>
        <p class="info-text">
            Haga clic en el siguiente enlace para generar el documento en PDF.
        </p>

        <div class="text-center">
            <form asp-action="listadoPersonasPdf" asp-controller="PDF">
                <input type="submit" class="btn btn-success" value="Generar PDF" />
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
